# 错误提示完善待办事项

更新时间：2026-07-27  
审计基线：81 个确认缺口（P0 7、P1 50、P2 24），合并整理为 77 个可执行事项  
清单进度：77 / 77（实现项均已勾选，最终发布门禁仍需单独验收）

## 完成定义

每项改动至少满足：失败对用户可见；文案说明发生了什么；提供可执行的恢复动作；技术原文仅作为详情；异步操作不会产生未处理 rejection；成功回执不会与失败状态混淆；补充对应自动化测试。

## P0：阻断、假成功与完全静默

- [x] 诊断页自检、字幕浮窗自检、刷新运行时失败时显示页面内错误反馈。
- [x] 诊断包导出显示成功路径和文件数，失败时显示具体原因。
- [x] 自动修复保留成功或失败结果，收尾刷新失败不能吞掉结果。
- [x] 实时会话页显示/隐藏字幕浮窗失败时显示横幅和 toast，并提供前往诊断页的恢复动作。`RealTimeSessionScreen.tsx`
- [x] 字幕浮窗设置页显示/隐藏失败时显示行内错误；同步覆盖浮窗自身的隐藏入口。`SubtitleOverlaySettingsPage.tsx`、`SubtitleOverlayScreen.tsx`
- [x] 不支持实时语音的模型启动会话时返回明确错误码，禁止以 capturing 状态假成功；增加“前往提供商设置”动作。`route_orchestrator.rs`、`session-error-presentation.ts`
- [x] 麦克风测试必须测试当前选中设备，以峰值阈值判断无信号，显示实际峰值和排查建议。`useAudioDeviceTestController.ts`

## P1：核心流程必须可恢复

### 实时会话与浮窗

- [x] 清空字幕失败时保留字幕并显示错误反馈。
- [x] 会话错误横幅中的“导出诊断”失败时显示错误，成功时显示产物路径。
- [x] 看片模式启动超时后的晚到成功/失败结果必须同步到可见状态。
- [x] 对话模式麦克风链路异常必须进入会话错误呈现通道。
- [x] TTS 播放失败必须显示可恢复提示，避免只记录日志。
- [x] “连接重试已用尽”后的重新启动失败必须更新横幅，而不是只写日志。
- [x] 浮窗右键菜单的隐藏/清空失败必须显示反馈。
- [x] 浮窗入口初始化失败必须呈现降级界面，不能留下空白窗口。
- [x] 浮窗锁定状态同步失败必须回滚 UI 并提示。

### Rust 实时链路

- [x] OpenAI Realtime 凭据失效、配额耗尽、重连用尽时发出统一错误事件。
- [x] Gemini Live 凭据失效或不可恢复断线时发出统一错误事件。
- [x] 腾讯实时语音鉴权失败或断线时发出统一错误事件。
- [x] 字幕二次翻译鉴权、模型或请求错误必须进入会话错误通道。
- [x] 二次翻译模型引用失效时禁止静默回退，并指向模型设置。
- [x] subtitle-translate worker 启动失败必须标记部分启动失败并停止假正常状态。
- [x] subtitle TTS worker 启动失败必须展示“字幕仍可用、语音不可用”的降级提示。
- [x] Omni 音色回退后的重连失败必须保留原始错误链和恢复建议。
- [x] 音频采集线程、Omni worker 退出时输出稳定错误码，不直接暴露英文 Rust/HTTP 原文。

### 启动与配置持久化

- [x] 配置草稿持久化失败时显示“未保存”状态、失败原因和重试入口。
- [x] 启动配置读取失败时明确说明正在使用回退配置，并提供恢复动作。
- [x] 诊断页的启动错误快照展示真实消息、错误码和请求号，替换泛化错误。
- [x] 启动进度弹窗增加 45 秒硬超时和阶段失败状态，禁止无限等待。
- [x] 全局启动 Promise rejection 必须落入可见启动错误界面。
- [x] IPC 首探耗尽后显示桌面运行时未接通，并提供立即重新连接按钮。
- [x] 首次运行向导驱动刷新失败时显示具体原因和重试入口。

### 提供商与凭据

- [x] 存储层恢复长时间未就绪时提供手动重试入口和具体错误。
- [x] 自定义提供商模板读取损坏时提示用户并保留原始可恢复数据。
- [x] 自定义提供商创建或自动保存失败时回滚状态并显示错误。
- [x] 存储恢复失败按超时、凭据、SQLite 分类呈现。
- [x] 首次运行向导读取密钥失败时使用本地化说明，不显示底层原文。
- [x] 首次运行向导保存/验证密钥失败时按鉴权、网络、配额分类。

### 音频与驱动

- [x] 驱动探测的部分失败不能显示“已就绪”，必须标明无法验证的项目。
- [x] 驱动安装、卸载、重装失败按 UAC 取消、脚本失败、超时分类，并保留重试操作。
- [x] 麦克风权限拒绝、设备拔出、独占占用等错误提供本地化解释。
- [x] 音频设备枚举失败时显示错误状态和刷新入口，不能只显示空列表。
- [x] 启动时设备刷新失败必须反映到音频路由页。
- [x] 扬声器测试必须验证 AudioContext 已运行、加入超时，并保证资源释放。

### 诊断与术语表

- [x] 实时事件读取失败与“暂无事件”使用不同状态，并保留刷新入口。
- [x] 模型基准测试错误按鉴权、网络、文件路径、模型能力分类，并保留技术详情。
- [x] 术语词条源术语/目标术语必填校验增加字段提示与字段错误标记。

### 国际化

- [x] 为 18 个非中英文语言补齐运行时、Bridge、凭据、模型探测和场景启动错误文案。
- [x] 日文、韩文、泰文首次运行向导的密钥错误替换泛化消息。
- [x] 日文、韩文、泰文提供商页保存、读取、验证错误提供恢复动作。
- [x] 非中英文术语冲突策略补充清晰后果说明。
- [x] 会话启动前置检查与分阶段启动失败全部走稳定错误码映射。

## P2：一致性、回执与低频异常

- [x] 术语冲突选择“跳过”后明确提示该词条未保存。
- [x] 术语表、基准报告、实时事件导出增加成功/失败回执并延迟释放 Object URL。
- [x] 自动修复成功但运行时刷新失败时保留修复结果并提示手动刷新。
- [x] 日志级别切换失败时回滚下拉框，显示原因和重试入口。
- [x] 浮窗解锁失败时回滚锁定状态并显示提示。
- [x] 会话错误 toast 中导出诊断失败时保留 toast 并显示原因。
- [x] Bridge 命名管道查询超时增加重启服务、重新检测等恢复动作。
- [x] WASAPI 非常见 HRESULT 映射为设备独占、格式不支持等用户可理解文案。
- [x] API Key 保存成功但后续状态刷新失败时区分“已保存/状态未知”。
- [x] 显示密钥失败时保留遮罩状态并显示错误。
- [x] 提供商操作错误提示区分保存、读取、验证三类操作。
- [x] 平台启用和排序持久化失败时回滚 UI。
- [x] 非中文会话运行时、停止失败和归因失败文案补齐。
- [x] 音频路由运行态异常增加诊断修复入口。
- [x] Bridge 自启失败在虚拟麦选项旁显示阻断原因。
- [x] 扬声器测试增加 suspended 状态恢复与硬超时。
- [x] 驱动安装、卸载、修复后的复检失败必须覆盖陈旧快照，禁止显示绿色成功。
- [x] 非中英文界面语言按需加载失败时回滚语言并提示。
- [x] 首次运行向导语言加载失败时回滚选择并提示。

## 工程治理

- [x] 建立统一错误呈现结构：`code`、`title`、`summary`、`technicalDetail`、`recoveryActions`、`source`。
- [x] 统一页面内反馈、横幅和 toast 的适用边界，避免通知写入后没有渲染出口。
- [x] 原生快照更新时保留尚未确认的本地通知，或将两类通知拆分存储。
- [x] 为 `void asyncCall()`、无 catch 的 `try/finally`、空 catch 和 log-only catch 增加静态扫描门禁。
- [x] 所有导出入口统一返回产物路径、文件数，并提供“打开所在目录”。
- [x] 错误码与全部 locale key 加入契约和平价测试。

## 分批验证

- 前端变更：`npm run verify:desktop`
- Tauri Rust 变更：`npm run test:desktop-shell`
- 跨进程错误码或事件变更：`npm run test:contracts`
- 配置路径变更：`npm run test:config-paths`
- 发布前：`npm run quality:gate:release`
