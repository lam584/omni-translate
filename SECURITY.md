# Security Policy

## 支持范围

Omni Translate 当前处于 `0.x` 开发阶段。安全修复仅面向默认分支 `main` 的最新代码；旧提交、个人分支、未发布构建和第三方修改版本不承诺维护。

| 版本 | 安全支持 |
| --- | --- |
| `main` / 最新发布版 | 支持 |
| 旧版本与非官方构建 | 不支持 |

## 私下报告漏洞

请不要通过公开 Issue、Discussion、Pull Request、日志附件或社交媒体披露尚未修复的漏洞。

首选方式是在仓库的 **Security** 页面使用 **Report a vulnerability**，创建私有漏洞报告：

<https://github.com/lam584/omni-translate/security/advisories/new>

如果该入口不可用，请发送邮件至 `ls20@vip.qq.com`，主题以 `[SECURITY] Omni Translate` 开头。请只发送复现所需的最少信息，不要发送真实 API Key、密码或无关个人数据。

报告尽量包含：

- 受影响的版本、提交或组件；
- 漏洞类型及潜在影响；
- 可重复的操作步骤或最小概念验证；
- 所需权限、前置条件和攻击面；
- 建议修复或缓解方案（如有）；
- 是否已经向其他人披露。

## 响应流程

以下时间是处理目标，不是服务等级协议：

- 3 个工作日内确认收到报告；
- 7 个工作日内完成初步分类并沟通下一步；
- 根据严重程度、修复复杂度和发布风险协调修复与披露时间。

维护者可能要求补充信息，并会尽量在修复发布前保持报告私密。修复完成后，可在征得报告者同意的情况下公开致谢。

## 重点安全领域

本项目尤其关注：

- Windows 虚拟音频驱动、IOCTL、命名管道和权限边界；
- Tauri 命令、事件、IPC 与 Native Bridge 消息验证；
- 安装、卸载、修复及 UAC 提权脚本；
- API Key、Windows Credential Manager、日志和诊断导出中的敏感信息；
- 音频或字幕数据未经授权的采集、持久化或传输；
- 更新、发布、签名和依赖供应链完整性；
- 可导致任意代码执行、权限提升、路径穿越、命令注入或拒绝服务的问题。

一般功能缺陷、模型输出质量、无法复现的性能问题以及不涉及安全边界的配置错误，请使用公开的 Bug 报告模板。

## 安全研究准则

请在你拥有或获准测试的系统上进行研究，避免访问他人数据、破坏设备状态、影响第三方服务或持续扩大漏洞影响。发现敏感数据后立即停止并报告。善意、守法并遵循本策略的研究者不会因报告本身受到追责。
