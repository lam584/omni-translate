// The sole native-window import boundary for the legacy overlay page.
export { LogicalPosition, LogicalSize } from '@tauri-apps/api/dpi';
export { Menu } from '@tauri-apps/api/menu';
export { currentMonitor, cursorPosition, getCurrentWindow, PhysicalPosition } from '@tauri-apps/api/window';
