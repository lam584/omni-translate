[package]
name = "omni-benchmark-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
base64 = "0.22"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints.rust]
unreachable_pub = "warn"
