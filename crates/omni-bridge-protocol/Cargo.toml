[package]
name = "omni-bridge-protocol"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
ts-rs = { version = "12", features = ["serde-compat"] }

[dev-dependencies]
serde_json = "1"

[lints.rust]
unreachable_pub = "warn"
