# Windows Virtual Audio Tools

Development install and repair are driven by `scripts/installer/*.ps1`.
`virtual-driver-render.pcm` is no longer a runtime sink. Native Bridge
diagnostics may write `last-translation-frame.pcm` under the runtime root.
