[package]
name = "credential-write-diagnostic"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
windows-sys = { version = "0.59", features = ["Win32_Foundation", "Win32_Security_Credentials"] }
